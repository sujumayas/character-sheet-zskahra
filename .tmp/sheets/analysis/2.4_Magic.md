# 2.4 Magic

## Purpose & state

**State: STUB / MISLABELED.** Despite the tab name "2.4 Magic", the content has nothing to do with magic, MP/Zeon, mystical paths, or spells. It is a 9-row fragment of a generic secondary-skill table (rows 2-5 labelled "Medicine", rows 6-10 labelled "Outdoors Technical" — note typo "Ourtdoors") with the same column structure used elsewhere in the workbook (Category / Skill / Stat / DP allocation columns / Cost / # Ranks / Ranks Value / Cat Value / Stat Value / Talent / Special / Total).

All input cells (G2:G10 ranks, J/K/L bonuses) are zero. No magic-system data exists on this tab:
- No Zeon pool, MP, magic accumulation (MA), magic projection, or magic level fields.
- No spell list, mystical-path allocation, free-access slots, metamagic, summoning, etc.
- No references to magic-related cells on other tabs.

The only "live" piece is `O1 ='Level Progression'!J3` (= 178), labelled "Available DPs to allocate" — i.e. the same DP-budget readout reused across skill tabs, here pointing at a single cell of Level Progression rather than a magic budget.

Conclusion: this tab appears to be a leftover scaffold / accidental duplicate of the secondary-skills layout, never populated with Anima magic mechanics. It can safely be treated as a stub.

## Inputs

| Cell | Meaning |
|------|---------|
| A2 | Category label: "Medicine" (merged A2:A5) |
| A6 | Category label: "Ourtdoors Technical" (sic) (merged A6:A10) |
| G2:G10 | # Ranks invested per skill row (all 0) |
| I2:I10 | Cat Value (category bonus) — hard-coded: -13 for Medicine rows, +7 for Outdoors Technical rows |
| J2:J10 | Stat Value (all 0, no formula — would need manual entry) |
| K2:K10 | Talent bonus (all 0) |
| L2:L10 | Special bonus (empty — referenced by M but has no value) |
| F2:F10 | Cost column, hard-coded as "-" (no DP cost shown for these rows) |

## Formulas

| Cell | Formula | Computes |
|------|---------|----------|
| O1 | `='Level Progression'!J3` | Pulls "Available DPs to allocate" (178) from Level Progression J3 |
| B2 | `=if(A2="",B1,A2)` | Skill label fallback: inherit category from row above if blank (used to fill merged-label rows) |
| B3..B10 | `=if(An="",Bn-1,An)` | Same fallback pattern, inheriting the merged category label |
| H2..H10 | `=if(or(Gn=0,Gn=""), 0, MIN(if(Gn<=10, Gn*3, 30) + if(Gn<=10, 0, if(Gn<=20,(Gn-10)*2,20)) + if(Gn<=20,0,(Gn-20))))` | "Ranks Value": tiered DP-cost-or-rank-conversion. First 10 ranks count ×3 (cap 30), ranks 11-20 count ×2 (cap +20), ranks 21+ count ×1. Wrapped in MIN() with a single argument (a no-op — likely a remnant of an earlier capped formula). |
| M2..M10 | `=Hn+In+Jn+Kn+Ln` | Total skill value = ranks value + category bonus + stat value + talent + special |

## Cross-tab refs

- `O1`: `='Level Progression'!J3` — source tab **Level Progression**, cell J3 (resolves to 178, character DP pool / available DPs).

No other cross-tab references. Notably, no references to Stats (Power/INT), Combat tab, Modules, or anything that would back a magic subsystem.

## DB mapping

This tab contributes **no magic-specific data** to map. The only structural content (skill rows, ranks, category/stat/talent/special breakdown, total) duplicates the layout already covered by the secondary-skills tabs and would map to the existing skill tables (`character_skills`, etc.) — but with all inputs zero and the labels duplicated/typoed, there is nothing meaningful to persist from this tab.

**Gap flag — magic system has no DB backing yet:**
- The current Supabase schema has **no** `spells`, `mystical_paths`, `magic_paths`, `summons`, `metamagic`, `free_access`, or `spell_list` tables.
- `game_values` (11 rows) may include Zeon, Magic Accumulation (MA), Magic Projection, Magic Level — these would naturally live in `character_game_values` (base + bonus → generated `total_value`) **only if** they were registered as game values. This tab does not confirm whether they are.
- `character_chaos` (chaos_index, chaos_track, chaos_dice, chaos_power_calculator) appears to be a **custom Zskahra subsystem** rather than Anima's Zeon-based magic. It may functionally replace traditional Anima magic in this campaign.
- `talents` (326 rows) likely contains some magic-school / metamagic talents that flow through `character_talents` + `character_talent_choices`.

Because this tab provides no magic data, the gap is not actionable from here — it must be assessed from whichever tab actually carries magic info (if any). If no other tab does, the campaign likely uses the **chaos** subsystem in place of conventional Anima magic.

## Recommendations

- **Defer this tab.** Ship the Next.js sheet without a "2.4 Magic" view; the data is empty and the layout is already covered by the secondary-skills page.
- **Do not create new magic tables yet.** With no source-of-truth content here, building `spells` / `mystical_paths` / `magic_levels` schemas would be speculative. Wait until a populated magic tab (or a clarifying conversation about the chaos system) appears.
- **Reuse `character_chaos` as the likely magic surrogate.** Given the dedicated `character_chaos` table (chaos_index, chaos_track, chaos_dice, chaos_power_calculator), the Zskahra campaign appears to substitute "chaos" for Anima Zeon magic. Any magic-adjacent UI should be built on top of `character_chaos` first.
- **If a future tab introduces real magic data** (Zeon pools, MA, paths, spell lists), extend via:
  - `game_values` rows for Zeon / MA / Magic Projection / Magic Level → backed by `character_game_values`.
  - New tables `mystical_paths`, `character_mystical_paths`, `spells`, `character_spells_known`, `character_free_access` once the data model is concrete.
- **Cosmetic:** the source sheet contains the typo "Ourtdoors Technical" — if any of these labels survive into the DB or UI seed data, normalize to "Outdoors Technical".
- **Note the no-op MIN() in H-column formula.** When porting the rank-cost formula, drop the outer `MIN(...)` (single argument) and just sum the three tier expressions for clarity.
